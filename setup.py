#!/usr/bin/env python

from distutils.core import setup

setup(name='oauth-bridge',
      version='0.1-1',
      description='Standalone OAuth redirect endpoint and cache for polling from local applications.',
      author='Toni Mattis',
      author_email='toni.mattis@hpi.de',
      url='https://github.com/amintos/oauth-bridge',
      packages=['oauth_bridge'],
     )